# 路上吃什么：MVP 产品与技术说明

## MVP 目标

在驾车场景下，根据起点、终点、出发时间和最晚到达时间，优先过滤会导致迟到的餐厅，再按口味、评分和便利性排序。

首版支持：

- 起点、沿途、终点三类候选区域；
- 高德地址解析、驾车路线和餐饮 POI 搜索；
- 每个候选点的二次算路；
- 绕路、停车、步行、排队和用餐的分项时间预算；
- 赶时间与休闲两种动态排序权重；
- 无地图密钥的离线样例与回归测试。

暂不包含：公交换乘、实时导航、下单、订座、真实排队时间、用户账号和长期偏好。

## 核心原则

1. 时间可行性先于口味和评分。
2. 绕路成本使用 `A → 餐厅 → B` 与 `A → B` 的实际路线差值，不使用直线距离代替。
3. 模型负责理解意图和追问；服务端负责算路、硬约束和排序。
4. 未验证的营业、排队和停车信息必须标为估算或未知。

## 运行方式

要求 Node.js 20 或更新版本。

- 离线验证：`npm test`
- 查看样例：`npm run demo`
- 实时高德查询：在插件运行环境设置 `AMAP_WEB_SERVICE_KEY`，调用 `plan_enroute_meal` 时不传 `directTravelMinutes` 和 `candidateRestaurants`。

插件通过 `.mcp.json` 启动本地 MCP 服务，不依赖第三方 Node 包。

## 下一阶段

1. 用 50—100 个真实行程建立离线评测集。
2. 接入营业状态、外卖取餐和停车数据，替换默认估算。
3. 记录匿名化预测误差：实际绕路、排队、到达时间。
4. 增加公交/地铁和机场、高铁的专用安全缓冲模型。
5. 增加地图结果卡片、路线对比和一键导航。

## 成功指标

- 准时到达率：硬性保护指标；
- 推荐采纳率；
- 预计绕路与实际绕路的绝对误差；
- 因关门、排队、停车导致的推荐失败率；
- 用户从提出需求到确认方案的时间。
